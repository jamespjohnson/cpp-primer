#include <vector>
#include <iostream>

int main()
{
	vector<int> v;
	auto b = v.begin(), e = v.end(); // b and e have the same type
	// you do not need to care about the precise type an iterator has.
}
